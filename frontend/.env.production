# Production builds (Vercel) call the API on the same origin; vercel.json proxies
# /api/* to the AWS ALB, so the browser never makes an HTTPS -> HTTP request.
# Temporary until CloudFront provides HTTPS for the API (see CLAUDE.md).
VITE_API_BASE_URL=/api/v1
